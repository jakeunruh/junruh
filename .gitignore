# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# production (CRA)
/build

# production (Vite)
/dist        # ✅ Vite builds to /dist, not /build

# Vite cache
.vite        # ✅ Vite's local cache directory

# misc
.DS_Store

# environment variables
.env.local
.env.development.local
.env.test.local
.env.production.local
.env          # ✅ Add this to avoid committing base .env file

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
